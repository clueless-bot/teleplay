import upload from "../../../src/src/utils/multerConfig";

export const onEditClicks = (uploadId, uploadData) => {
    // Handle edit click logic here, e.g., open a modal or navigate to an edit page
    let id = uploadId;
    let data = uploadData;
  }